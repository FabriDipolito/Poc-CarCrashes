spring.application.name=demo

# JPA/Hibernate Configuration

spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLiteDialect
spring.jpa.properties.hibernate.hbm2ddl.auto=update
# hibernate ddl-auto --> create-drop, creates and drops the tables once finished backend, update persist de tables

#SQLite configuration

spring.datasource.url=${DB_PATH}
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.username=sa
spring.datasource.password=sa

#Mail Sender configuration

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=carcrashespoc@gmail.com
spring.mail.password=elpk huim kmbj dzfy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Profiles
spring.profiles.active=${SPRING_PROFILES_ACTIVE}
